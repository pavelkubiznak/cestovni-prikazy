// Supabase connection — anon (public) key is safe to ship in a client app.
// Data is protected by Row Level Security: each user only sees their own row.
window.__SB = {
  url: "https://fpknbrzbqpalguajskut.supabase.co",
  anon: "REPLACE_WITH_ANON_KEY"
};
